# Screenshots Needed

No screenshots are committed because the project uses source-controlled Mermaid diagrams and a live local UI.

Recommended captures for a portfolio listing:

1. Dashboard connected to Savanna: plan/usage cards, jobs and flag list.
2. Platform-admin tenant table with all three explicitly fictional organizations.
3. Terminal output from `scripts/demo.ps1` showing the expected cross-tenant 404.
4. Test summary showing both unit and integration assemblies green.
5. OpenAPI JSON or `/docs` landing page.
6. Code split view: `FieldOpsDbContext` global filters and `TenantSaveChangesInterceptor`.
7. Audit viewer after toggling a flag.
8. A billing replay response with `duplicate: true`.

Before publishing, blur machine-specific paths if desired. Do not imply the screenshots show production traffic or real customers.
