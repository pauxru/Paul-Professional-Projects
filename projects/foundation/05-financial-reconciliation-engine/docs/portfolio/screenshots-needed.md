# Screenshots and Recordings Needed

Capture these assets after running the local demo with fictional data. Use filenames consistently so the portfolio page can link to them later.

| Filename | What it shows | Why it matters |
|---|---|---|
| `01-openapi-ui.png` | OpenAPI UI at `http://localhost:5005/openapi`, with imports, runs, exceptions, reports, health, and auth routes visible. | Proves the API is discoverable and presents a realistic service surface. |
| `02-run-report-json.png` | JSON response from `GET /api/v1/runs/{runId}/report`. | Shows the reconciliation output: run metadata, counts, totals, exception breakdown, and immutable run reporting. |
| `03-balance-assertion-json.png` | JSON response from `GET /api/v1/reports/runs/{runId}/balance`. | Demonstrates the self-checking balance assertion that fails loudly if internal totals do not equal matched plus unmatched totals per currency. |
| `04-aging-report.png` | `GET /api/v1/reports/aging?format=json` or CSV opened in a terminal/table view. | Shows operational exception management: open exceptions grouped into aging buckets. |
| `05-exception-audit-trail.png` | `GET /api/v1/exceptions/{id}` after assign, resolve, and approve actions. | Proves four-eyes workflow and immutable audit history for financial operations. |
| `06-perf-harness-output.png` | Output from `dotnet run -c Release --project src\ReconEngine.PerfHarness -- --sizes 10000,100000,250000`. | Captures the verified performance story: 230k+ rows/s reconciliation throughput and indexed matching speed-up. |
| `07-passing-tests.png` | Terminal output from `dotnet test -c Release`. | Provides engineering credibility: 118 passing tests across unit and integration suites. |
| `08-datagen-manifest.png` | `manifest.json` generated by DataGen, ideally beside the generated `internal.csv`, `external.csv`, and `external.fixed.txt` files. | Shows deterministic synthetic data with ground-truth defect counts, used to validate exact exception behavior. |
| `09-import-results.png` | Responses from `POST /api/v1/imports` for internal CSV and external CSV or fixed-width input. | Demonstrates streaming ingestion, checksums, accepted rows, rejected rows, and profile-driven parsing. |
| `10-four-eyes-approval-flow.gif` | Short recording of assign → resolve with `WriteOff` → approve with a different user. | Makes the maker-checker control easy to understand for non-technical viewers. |

Notes:

- Use only fictional demo data from DataGen.
- Do not imply real clients, production usage, uptime, revenue, certifications, or live bank data.
- Use **KES** examples when highlighting write-off thresholds or Kenyan fintech context.
