# Screenshots Needed

No screenshots are committed because the primary proof is text evidence. For a portfolio presentation, capture:

1. Swagger UI at `/docs` showing the protected order endpoint.
2. A terminal run of `INC-001` broken and fixed with generated evidence paths.
3. Side-by-side `INC-002` evidence showing `SCAN CargoEvents` and `SEARCH ... USING INDEX`.
4. The JSON comparison for `INC-008` downstream call amplification.
5. A passing `dotnet test -c Release` summary.

Redact bearer tokens, local filesystem usernames, and any environmental configuration before publishing.
