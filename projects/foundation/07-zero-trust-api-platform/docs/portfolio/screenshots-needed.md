# Screenshots Needed

For a static portfolio page (e.g. GitHub Pages, personal site) the following screenshots
would tell the story. All are generated by running the code — nothing fabricated.

## 1. Green test run

- Terminal after `dotnet test -c Release`.
- Both project summaries visible: ZeroTrust.UnitTests (27 passed) and
  ZeroTrust.IntegrationTests (38 passed).

## 2. JWKS with two active keys

- Browser or `curl` output of `http://localhost:5007/.well-known/jwks.json`.
- Two entries with distinct `kid`.
- Response is captured immediately after `POST /api/v1/admin/keys/rotate`.

## 3. Explainable authz endpoint

- `POST /api/v1/admin/authz/evaluate` response for a case where the deciding requirement
  is the ownership check. Shows the deciding requirement, allowed=false, why="ownership".

## 4. IDOR denial (Alice trying to read Bob's account)

- Response body: RFC 7807 ProblemDetails, status 403.
- Audit trail row appearing immediately with `AuthorizationDeny`.

## 5. Refresh reuse detected → whole family revoked

- Sequence of three requests: refresh #1 (success), refresh #2 with the *same* token
  (also success in a hijack scenario), refresh #3 with the *first* token again (denied,
  entire family revoked). Include the audit log rows showing family revocation.

## 6. Rate limiter 429 with Retry-After

- Burst against `/api/v1/partner/payments/{guid}` on the low-limit factory or a partner
  with low permits. First responses are 200/404, then 429 with `Retry-After: 60`.

## 7. Break-glass full flow

- Three requests: request grant, approve (from different admin), use. Then a fourth
  request that violates the two-person rule and returns 422.

## 8. Audit chain verification

- `GET /api/v1/admin/audit/verify-chain` → `{ ok: true }` before tampering.
- Manually edit one audit row in SQLite (`UPDATE audit_records SET Detail = 'tampered' WHERE Sequence = 5;`).
- `GET /api/v1/admin/audit/verify-chain` → `{ ok: false, firstFailingRow: 5 }`.

## 9. API-key cutover

- Migration report before cutover.
- Toggle POST.
- Legacy key request now returns 401 `api_key_past_cutover`.
- Migration report after cutover shows the rejection count.

## 10. Architecture diagram

- Render `docs/architecture/architecture.md` in a Mermaid preview. Screenshot the top
  container diagram plus the customer-token sequence.

## Notes

- No production screenshots — everything is local, and clearly labelled `localhost:5007`.
- Redact bearer values from any screenshot (blur the middle segment). Refresh tokens
  never appear in screenshots.
