# Screenshots Needed

All screenshots must show synthetic data only.

1. Swagger `/docs` endpoint groups and local API port.
2. Application JSON after screening, expanded `decisionTrace.rules` and `riskAssessment.contributions`.
3. Underwriting queue showing priority, SLA due time, and claim lock fields.
4. Offer JSON showing schedule and accepted immutable state.
5. Audit JSON with actor/action/resource/correlation/hash chain fields.
6. What-if report showing flip count and per-application delta.
7. `dotnet test -c Release` summary.
