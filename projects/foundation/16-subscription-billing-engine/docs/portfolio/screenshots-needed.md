# Screenshots Needed

Capture only synthetic data after the repository is public-ready.

1. Swagger UI showing the versioned endpoint groups.
2. Invoice preview response with reconciled credit and charge lines.
3. Usage first response (`duplicate: false`) and replay (`duplicate: true`).
4. Two invoice-run responses plus one resulting invoice.
5. Invoice HTML rendering for Savanna Logistics Ltd (fictional).
6. Failed payment response and PastDue/dunning state.
7. Signed recovery webhook followed by Paid invoice.
8. Test terminal with final passed/failed/skipped counts.
9. Mermaid architecture and dunning diagrams rendered on GitHub.
10. SQLite schema/unique constraints using a local schema viewer, with no machine paths or secrets visible.

Redact bearer tokens, signatures, local usernames, absolute paths, and any non-fictional data.
