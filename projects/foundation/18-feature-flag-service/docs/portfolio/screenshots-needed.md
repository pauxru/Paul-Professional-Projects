# Screenshots Needed

No screenshots are committed because this project uses Mermaid documentation and reviewers can run the service locally. Capture these only from synthetic data:

1. Admin UI flag list with dev/staging/production selector.
2. Targeting preview showing a `SegmentMatch` reason.
3. Audit timeline with before/after diff and revert action.
4. Production approval request and distinct reviewer action.
5. Demo consumer `/checkout` response showing variation/reason.
6. Browser network view of SSE `event: config` followed by ETag config fetch.
7. Stale-flag technical-debt report.

Redact bearer tokens, SDK keys, context values, filesystem paths, and any browser extensions before publication.
