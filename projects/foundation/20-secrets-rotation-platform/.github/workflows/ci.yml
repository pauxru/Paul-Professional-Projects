name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore
        run: dotnet restore Northstar.SecretsRotation.sln
      - name: Build
        run: dotnet build Northstar.SecretsRotation.sln -c Release --no-restore
      - name: Test
        run: dotnet test Northstar.SecretsRotation.sln -c Release --no-build
