# Screenshots Needed

No screenshots are committed because this repository is API-first. Capture these locally for a portfolio presentation:

1. `/api/v1/analyze` response showing `<b>Café laptops</b>` becoming tokens with offsets.
2. Hybrid search response showing highlighted hit, score explanation, hierarchical facets, `didYouMean`, and cursor.
3. OpenAPI document at `/openapi/v1.json` in a viewer.
4. Alias list before/after a `catalogue-v1` to `catalogue-v2` swap.
5. `/api/v1/analytics/summary` after synthetic click events.
6. Terminal output for `--evaluate` and `--benchmark`.

Redact bearer tokens and keep all data labelled fictional Contoso Retail.
