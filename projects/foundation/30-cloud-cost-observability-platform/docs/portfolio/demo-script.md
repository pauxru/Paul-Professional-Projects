# Demo Script

1. Run `scripts\demo.ps1`; open `http://localhost:5030`.
2. Show `/api/v1/tags/coverage` and explain remediation of deliberate missing tags.
3. Show costs in USD versus `reportingCurrency=KES`.
4. Open allocations, then drill into `/audit/{costRecordId}` to show direct tag, parent, shared, or residual explanation.
5. Show budget status and forecast methods; point to measured MAPE selection.
6. Show grouped anomalies and the scheduled batch suppression.
7. Review recommendations, accept/implement/verify one with baseline/post-change values.
8. Show unit cost per order/tenant/GB and explain why a business denominator changes the discussion.

All identities, resources, FX rates, and costs are fictional synthetic data.
